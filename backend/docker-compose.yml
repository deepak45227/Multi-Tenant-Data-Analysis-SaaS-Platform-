version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: my-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data     # optional: keep data after restart
    restart: unless-stopped

volumes:
  redis-data: